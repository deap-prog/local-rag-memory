# --- INFRASTRUCTURE LOCALE (Racines) ---
# Utilise les "container_name" définis dans le docker-compose (valeurs réseaux Docker)
ANYTHING_LLM_HOST=http://ia-anythingllm:3001
OLLAMA_HOST=http://ia-ollama:11434

# --- Pour tests locaux (optionnel) : URLs vers les services exposés sur l'hôte
# Exemple : si vous utilisez les ports hôtes ci-dessus, vous pouvez définir :
# ANYTHING_LLM_HOST_LOCAL=http://localhost:23001
# LITELLM_HOST_LOCAL=http://localhost:23002

# --- HOST PORT MAPPINGS (modifiable) ---
# These control which host ports are bound to the container ports.
# You can change them if ports conflict on your machine. Example defaults below.
# Note: docker-compose will use the env variables LITELLM_PORT_HOST etc. when present.
LITELLM_PORT_HOST=23002      # host:container -> 23002:4000
OPENWEBUI_PORT_HOST=23003    # host:container -> 23003:8080
ANYTHING_PORT_HOST=23001     # host:container -> 23001:3001
OLLAMA_PORT_HOST=23004       # host:container -> 23004:11434

# --- MOTEUR IA (LiteLLM) ---
# Lui on le garde tel quel car c'est l'endpoint OpenAI standard Utilise le container_name
LITELLM_URL=http://ia-litellm:4000/v1

# --- PARAMETRES WORKER, Format HH:MM (24h) ---
SUMMARY_TIME=04:00
WORKSPACE_SLUG=night
BASE_MODEL=ollama/mistral
# NOUVELLE VARIABLE : Temps max (en secondes) pour attendre le CPU
# 600 secondes = 10 minutes. Si ton CPU met plus de temps, on coupe.
LLM_TIMEOUT=600

# --- ARCHIVAGE (Worker) ---
# Contrôle la fenêtre historique que le worker va archiver.
# - Priorité à SINCE_MS si défini (timestamp epoch en millisecondes).
#   Exemple pour reprocesser tout (usage dangereux) : SINCE_MS=0
#   Exemple pour cutoff précis : SINCE_MS=1672531200000
# - Si SINCE_MS absent, on utilise SINCE_DAYS (entier, défaut=1)
#   Exemple pour les 2 derniers jours : SINCE_DAYS=2
# Remarque : laisser la variable absente ou commentée -> comportement par défaut (1 jour)
SINCE_MS=
SINCE_DAYS=1

# --- CLEFS API ---
# Entrez votre clé API Google ici (commence par AIza...)
GEMINI_API_KEY=AIza
# Entrez votre clé API DEEPSEEK ici (commence par sk-...)
DEEPSEEK_API_KEY=sk-
# Entrez votre clé API GROQ ici (commence par gsk_...)
GROQ_API_KEY=gsk_
# Entrez votre clé ANYTHING_LLM ici (commence par ...)
ANYTHING_LLM_API_KEY=
# Entrez votre clé TAVILY ici (commence par tvly)
TAVILY_API_KEY=tvly
# Entrez votre clé OPENAI ici (commence par sk-...)
OPENAI_API_KEY=sk-fake
